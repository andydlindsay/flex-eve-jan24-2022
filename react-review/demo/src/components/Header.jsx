// import React from 'react';

const Header = (props) => {
  // console.log(props);

  // const {message} = props;

  return (
    <div>
      <h2>{props.message}</h2>
    </div>
  );
};

export default Header;
